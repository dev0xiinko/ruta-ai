# Patch Notes

- Added a runnable sample dataset scaffold for the Python backend.
- Included direct, walk-only, walk-plus-ride, ride-plus-walk, and transfer examples.
- Expanded the backend dataset from the repo seed sources to 66 routes and 99 places.
- Added bootstrap generation via `ruta_python_backend/scripts/build_dataset_from_repo.py`.
